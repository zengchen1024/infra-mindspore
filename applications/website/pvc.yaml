---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: website-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
  storageClassName: nfs-rw
  volumeMode: Filesystem
