apiVersion: v1
kind: ConfigMap
metadata:
  name: configmap
data:
  config.yaml: |
    config_items:
    - repos:
      - src-openeuler
      clear_labels:
      - ci_successful
      - kind/wait_for_update
