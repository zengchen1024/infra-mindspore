---
apiVersion: v1
kind: Service
metadata:
  name: citoolservice
spec:
  ports:
    - name: httpport
      protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: cibotwebsite
  type: ClusterIP
