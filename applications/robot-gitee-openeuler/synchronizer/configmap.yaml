apiVersion: v1
kind: ConfigMap
metadata:
  name: configmap
data:
  config.yaml: |
    config_items:
    - repos:
      - zengchen1024/repo-test
      enable_sync_issue: true
      enable_sync_comment: true
      sync_org_mapping:
        zengchen1024: xwz123