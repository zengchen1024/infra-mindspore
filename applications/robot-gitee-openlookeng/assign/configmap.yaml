apiVersion: v1
kind: ConfigMap
metadata:
  name: configmap
data:
  config.yaml: |
    config_items:
    - repos:
      - openlookeng
      enable_pr_assign: true
      enable_issue_assign: true
      enable_issue_collaborator: true
